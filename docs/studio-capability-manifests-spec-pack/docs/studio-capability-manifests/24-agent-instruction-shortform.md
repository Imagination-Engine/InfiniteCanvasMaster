# 24 — Agent Instruction Shortform

```md
Do not implement randomly.
Read /docs/studio-capability-manifests/.
Create the current capability matrix first.
Every studio is a manifest. The canvas can mix elements from any studio.
Do not install tools before ToolMount evaluation.
Use Gemini model aliases, not hardcoded model names.
Use Mastra as the agent/workflow substrate.
Use MCP as a tool/resource/prompt protocol when appropriate.
Use ImagiClaw as product-facing name, not OpenClaw.
Do not expose OpenCove as a product-facing block.
Commerce/Wallet/Checkout/Intentcasting are demo-choreographed unless real connectors exist.
Every block needs capabilities, accepts, produces, studioAffinity, modelAliases, toolMountIds, runtime readiness, fabric lanes, security class.
Every tool needs mount mode, adapter, permissions, runtime readiness, license/source notes.
Implement in tracks. Stop after each track.
```
