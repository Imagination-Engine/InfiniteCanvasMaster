export declare function useViewportCamera(): {
  viewport: {
    x: number;
    y: number;
    zoom: number;
  };
  pan: (dx: number, dy: number) => void;
  zoomAt: (delta: number, centerX: number, centerY: number) => void;
};
//# sourceMappingURL=useViewportCamera.d.ts.map
